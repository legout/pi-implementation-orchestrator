---
description: Execute an approved plan with implementer and reviewer orchestration (orchestrate-implementation skill)
argument-hint: "[<plan or approved source>]"
---
Load the `orchestrate-implementation` skill — read its `SKILL.md` in full — and follow it exactly for this request. Do not improvise a substitute procedure; in particular, keep its readiness checks and approval gates untouched.

Request: ${@:-none given — the skill locates and verifies the approved plan.}

If the `orchestrate-implementation` skill is not installed in this session, stop and tell the user to run `/setup-implementation-orchestrator` first.
