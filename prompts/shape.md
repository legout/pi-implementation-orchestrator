---
description: Shape an idea into an approved design and spec (shape-design skill)
argument-hint: "<feature or idea>"
---
Load the `shape-design` skill — read its `SKILL.md` in full — and follow it exactly for this request. Do not improvise a substitute procedure.

Request: ${@:-none given — apply the skill's own intake questions.}

If the `shape-design` skill is not installed in this session, stop and tell the user to run `/setup-implementation-orchestrator` first.
