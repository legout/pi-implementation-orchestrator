---
description: Turn an approved spec into an executable implementation plan (write-implementation-plan skill)
argument-hint: "[<spec or approved source>]"
---
Load the `write-implementation-plan` skill — read its `SKILL.md` in full — and follow it exactly for this request. Do not improvise a substitute procedure.

Request: ${@:-none given — the skill asks for the approved behavioral source.}

If the `write-implementation-plan` skill is not installed in this session, stop and tell the user to run `/setup-implementation-orchestrator` first.
